
@model IEnumerable<Assignment>
@{
    ViewData["Title"] = "Assignments";
}

<h1>Assignments</h1>

<form asp-action="List" method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" name="name" value=@ViewBag.SearchString />
            <span>
                <button class="btn btn-default">
                    <i class="fa fa-search"></i>
                </button>
            </span>
        </div>
    </div>
</form>

@if (ViewBag.Message != null)
{
    <div class="alert alert-info">@ViewBag.Message</div>
}

@*<a asp-controller="Assignment" asp-action="Edit" class="btn btn-primary">Add New Assignment</a>*@

@foreach (var assignment in Model)
{
    <div class="card">
        <div class="card-body">
            <h3 class="card-title">@assignment.ClientName</h3>
            <span>Start Date: @assignment.StartDate</span>
            <span>End Date: @assignment.EndDate</span>
        </div>
        @*<div class="card-footer">
            <a class="btn btn-lg"
               asp-controller="Assignment"
               asp-action="Details"
               asp-route-id="@assignment.AssignmentId">
                <i class="fa fa-search-plus"></i>
            </a>
            <a class="btn btn-lg"
               asp-controller="Assignment"
               asp-action="Edit"
               asp-route-id="@assignment.AssignmentId">
                <i class="fa fa-edit"></i>
            </a>
            <a class="btn btn-lg"
               asp-controller="Assignment"
               asp-action="Delete"
               asp-route-id="@assignment.AssignmentId">
                <i class="fa fa-trash"></i>
            </a>
        </div>*@

    </div>
}

@*<a asp-controller="Assignment" asp-action="Edit" class="btn btn-primary">Add New Assignment</a>*@